Fact-Checker Agent (Task-3)

The Fact-Checker Agent is responsible for analyzing news articles generated by the Reporter Agent and determining their credibility using rule-based heuristics.

Responsibilities:
Receive news articles from the Reporter Agent
Analyze news content (headline + body)
Evaluate credibility based on:
Trusted vs untrusted sources
Presence of misinformation entities
Vague or unreliable claims
Assign a credibility score (0–100)
Flag suspicious claims and sources
Return a final verdict:
    PASS (credible)
    WARN (partially reliable)
    FAIL (not credible)


How to Run
1. Create Virtual Environment
python -m venv venv
2. Activate Environment
venv\Scripts\activate   
3. Install Dependencies
pip install -r requirements.txt
4. Run Server
python run.py


API Access
Open Swagger UI:
http://127.0.0.1:8000/docs

Use:
POST /fact-check   (to test the agent)


Logs
All runs are stored in:
logs/runs.json